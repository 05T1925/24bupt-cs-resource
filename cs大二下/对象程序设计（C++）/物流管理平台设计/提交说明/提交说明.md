# 综合实验提交说明

## 基本信息

- 姓名：刘文涛
- 学号：2024212936
- 压缩包：`刘文涛-2024212936-综合实验.zip`
- 解压后首层文件夹：`刘文涛-2024212936-综合实验/`
- 正式报告：`实验报告.docx`
- 辅助查阅报告：`实验报告.md`

## 三个独立程序

1. `题目一-物流业务管理与用户管理系统/`
   - 入口：`src/main.cpp`
   - 构建：`build.bat`
2. `题目二-快递员任务管理系统/`
   - 入口：`src/main.cpp`
   - 构建：`build.bat`
3. `题目三-物流管理系统网络版/`
   - 服务端入口：`server/main.cpp`
   - 客户端入口：`client/main.cpp`
   - 构建：`build_all.bat`

三个目录物理隔离，各自保留独立源码、构建脚本和数据文件；题目一、二不依赖题目三目录。

## 编译运行环境

- Windows
- MinGW-w64 g++ 8.1.0
- C++17
- 题目三使用 Winsock2，链接 `ws2_32`

最终编译与运行验证结果如下，验证日期为 2026-06-18。

## 创新点摘要

项目真实实现了加盐 SHA-256 密码、输入校验、原子保存、日志哈希链、智能任务分配、统计与绩效、token 鉴权、自定义 TCP 协议、并发冲突保护、通知、改派、备注和评分等增强功能。

## 需要注意

- 提交包不包含 `.exe`、`.o`、`.bak`、开发日志、测试临时目录或缓存；请在目标机器先执行构建脚本。
- 数据目录保留了正式数据文件，便于演示持久化；执行网络自测会追加测试数据与日志。
- V1/V2 源码为便于课程提交保留单文件组织，内部已经按类职责分层。
- V2 源码采用项目既有 GBK 编码口径，勿直接改为 UTF-8 后继续使用原构建参数。

## 验证记录

- 题目一编译：成功，`build.bat` 退出码 0。
- 题目二编译：成功，`build.bat` 退出码 0。
- 题目三服务端编译：成功，`build_server.bat` 退出码 0。
- 题目三客户端编译：成功，`build_client.bat` 退出码 0。
- 题目一运行：成功启动，输入 `0` 后正常退出。
- 题目二运行：成功启动，显示三身份主菜单，输入 `0` 后正常退出。
- 题目三运行：服务器以 `--once` 启动成功；客户端 `--selftest-user` 完成注册、登录、充值、易碎品寄件、越权签收拦截和本人快递查询，客户端及服务器退出码均为 0。
- 未在本轮重复执行：管理员自测、快递员自测和双客户端并发自测；相关自测入口与测试说明保留在源码、`docs/` 和 `tests/` 中。
