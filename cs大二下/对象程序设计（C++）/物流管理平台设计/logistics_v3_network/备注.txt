双客户端并发抢同一单
Token 零信任权限隔离
揽收资金与状态逻辑事务  回滚
多线程服务端与断线会话清理


可靠持久化与哈希链审计     三阶段原子文件保存  日志 SHA-256 哈希链
单文件 .tmp -> .bak -> 正式文件替换。
操作日志包含序号、前哈希和当前哈希。
管理员可在线执行哈希链校验。

资金一致性与回滚
这是应用层逻辑事务和内存回滚，不是数据库事务；多个数据文件之间不能宣称完全原子。

last：
多态服务端计费。
智能自动分配。
批量揽收、批量签收及逐单结果。
通知中心与未读计数。
快递改派及新旧快递员双向通知。
快递评分与快递员绩效。
余额不足后即时充值并重试。
登录失败三次自动冻结普通用户和快递员。

自动分配：未完成任务少、收入低、用户名排序。
批量揽收、批量签收，逐单返回结果。
站内通知中心和未读数。
改派、备注、评分、个人信息管理。
余额不足后可即时充值并重试。

过滤冻结或停用快递员后，依次比较：
1. 未完成任务更少。
2. 收入更低。
3. 用户名词典序更小。
兼顾负载均衡、收入公平和结果确定性。